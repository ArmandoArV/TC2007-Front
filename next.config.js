/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export"
};

module.exports = nextConfig;
module.exports = {
  images: {
    domains: ["laventa-bucket.s3.amazonaws.com"],
  },
};
